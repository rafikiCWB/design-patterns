package patterns.Structural.decorator;

/**
 * O padrão decorador requer uma classe decorativa abstrata que implemente a interface abstrata para o objeto que está sendo empacotado. Nesse caso, chamamos nosso decorador BoeingDecoratore fazemos com que ele implemente a IAircraftinterface.
 */
public abstract class BoeingDecorator implements IAircraft {

}
